from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='ML pipeline to predict btc price over 24h',
    author='Baris',
    license='MIT',
)
